# Backend API

## Setup

1. Copy env template:
   - `cp .env.example .env`
2. Install dependencies:
   - `npm install`
3. Start the API:
   - `npm run dev`

## Available endpoints

- `GET /api/health`
- `GET /api/goals`
- `GET /api/users`
- `GET /api/progress`
