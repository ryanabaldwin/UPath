# Frontend

This folder is reserved for all frontend (React/Vite) application code.

Current app source exists in the repository root (for example `src/`, `public/`, and Vite config files). Move those files into this folder when you are ready to fully split frontend and backend into separate apps.
